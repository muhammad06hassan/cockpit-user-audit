# Cockpit User Audit – Refactored Script

Refactored audit script to analyze cockpit user access,
internal users, and offboarded users.

## Local input files (not committed)

- internal_users.json
- profiles.json
- offboarded_users.csv

## Usage

````bash
python3 audit_refactor.py

#Input files are intentionally ignored by git.


Then:
```bash
git add README.md
````
